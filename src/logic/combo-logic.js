import { Combination } from "js-combinatorics";
import combos from "combos";
import { uniqWith, isEqual, sample } from "lodash";

// aim to be able to do this without rewriting for every color added, map through colour genes

// objects to be supplied by color logic

const combineColorResults = ([objects]) => {
  // combine all arrays etc into one object, separated by color
};

const splitGenesOfColor = (object) => {
  return combos({});
};

// input an object with any gene arrays
// return combos and split the gene arrays (put into a sub object? Or maybe put other details into a sub object)

const assignColor = () => {
  return color;
};

// input object, output color, visible/not visible, and base color

const breedHorses = (horse1, horse2) => {
  // join all gene arrays
  // output all combos (for breeding)
  // output all unique combos (for table)
  // combine
  // output result for all (for breeding)
  // output and sort unique result for table
};

const setTable = (result = {
  // add color
  // add images
});
